Forgot Password works in Replit Preview but does NOT work on the live published site after deployment.

Investigate and fix the production-only issue.

IMPORTANT:

* Do NOT rebuild the whole auth system
* Do NOT redesign UI
* Do NOT change unrelated routes
* Only diagnose why Preview works but Live does not

Likely causes to check:

1. Production build is stale

   * Run split/build steps if needed
   * Ensure the new /reset-password page is included in the production build
   * Ensure latest frontend dist is what Replit Publish serves

2. API route mismatch in production

   * Preview uses /api/askimate/auth/forgot-password
   * Confirm live site calls the same endpoint
   * Confirm live API server has the new route deployed

3. Reset link base URL wrong

   * Email reset link may point to preview/dev URL instead of https://universitio.com/reset-password?token=...
   * Ensure production email uses the correct public site URL:
     https://universitio.com

4. Environment variables missing in production

   * Check any required APP_URL / PUBLIC_BASE_URL / FRONTEND_URL / SITE_URL setting
   * Production email should not use localhost or Replit preview URL

5. Static routing issue

   * /reset-password must work as a client-side route in production
   * If direct access to /reset-password?token=... returns 404, configure fallback to index.html

6. Database migration

   * Confirm production DB has:
     passwordResetToken
     passwordResetExpiresAt
   * Preview and live must use same schema

Checks to perform:

* On live site, submit forgot password and inspect network response
* Check API logs during live forgot-password request
* Click email reset link and verify it opens the live reset-password page
* Test direct live URL:
  https://universitio.com/reset-password?token=test
  It should render the reset password page, not 404

Final result:

* Forgot password request works on live site
* User receives email
* Email link points to https://universitio.com/reset-password?token=...
* Reset password page loads on live site
* New password is accepted
* User can log in with new password
