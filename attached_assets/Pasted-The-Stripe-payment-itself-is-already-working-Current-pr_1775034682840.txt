The Stripe payment itself is already working.

Current problem:
- User can click a package
- User goes to Stripe checkout
- Payment succeeds
- BUT after payment, the system does NOT properly return and fulfill the purchase inside the app
- The user account/profile is NOT updated correctly based on the purchased package

This is the MOST IMPORTANT missing piece.

Do NOT redesign the page.
Do NOT break the current landing page, package cards, or chat flows.
Fix the payment fulfillment flow properly.

==================================================
GOAL
==================================================

After successful Stripe payment:

1. the system must know exactly which package was purchased
2. the user must return to the app correctly
3. the user account must be updated based on that package
4. the profile/subscription/dashboard must immediately reflect the new purchased plan
5. the user usage/credits/messages must be charged/allocated according to the package

This must be implemented as a real fulfillment flow, not just a successful payment redirect.

==================================================
REQUIRED PAYMENT FLOW
==================================================

Implement a proper Stripe success/fulfillment flow.

Required sequence:

1. User selects package
2. Stripe checkout session is created with enough metadata to identify:
   - user
   - package/plan
   - pricing tier
   - any relevant usage allocation
3. User completes payment successfully
4. Stripe success must lead to a proper app return flow
5. On return (or via webhook + confirmation flow), the app/backend must:
   - verify the successful purchase
   - identify which package was bought
   - update the user account
   - apply the purchased plan/credits/usage
6. The user must then see the updated plan in the app

==================================================
IMPORTANT IMPLEMENTATION REQUIREMENTS
==================================================

1. STRIPE METADATA
When creating the Stripe checkout session, include enough metadata to identify the purchase:
- userId
- packageId / planKey / tier
- any credits/messages allocation reference
- anything needed for post-payment fulfillment

2. DO NOT RELY ONLY ON FRONTEND QUERY PARAMS
Frontend redirect alone is not enough.
The system must have a reliable backend fulfillment step.

3. WEBHOOK OR VERIFIED SUCCESS FLOW
Implement the proper safe flow for fulfillment.
Preferred:
- Stripe webhook handles successful checkout completion
OR
- a verified backend success endpoint finalizes the purchase safely

If webhook support already exists, use/fix it.
If webhook is missing, add the minimal correct implementation.

4. UPDATE USER ACCOUNT
After successful payment, update the user account/profile/subscription data with:
- purchased plan
- plan status
- usage/messages/credits allocation
- anything needed for the current package logic

5. RETURN USER TO APP IN A USEFUL WAY
After successful payment:
- return the user to an appropriate app page
- ideally profile/subscription/dashboard
- show success state clearly
- reflect the new plan immediately

6. REAL-TIME / IMMEDIATE UI UPDATE
After return:
- the user should not need to manually refresh multiple times
- fetch the latest subscription/account state
- show the new purchased package clearly

==================================================
SHOW THIS TO THE USER
==================================================

After successful purchase, show clearly in the logged-in app:
- current plan
- purchased package name
- current allowance / remaining usage / messages / credits
- subscription/account status

This should appear in the relevant user-facing areas such as:
- profile
- subscription/account section
- dashboard, if appropriate

==================================================
FAILURE HANDLING
==================================================

Also handle these safely:
- payment success but account not yet updated
- payment canceled
- duplicate webhook/event handling
- already-processed payment event
- refresh after redirect

Make the fulfillment idempotent and safe.

==================================================
STRICT RULES
==================================================

- Do NOT break the working Stripe checkout initiation
- Do NOT break existing user dashboard/app redesign
- Do NOT fake success without real account update
- Do NOT only change UI text
- This must be real payment-to-account fulfillment

==================================================
SUCCESS CRITERIA
==================================================

After implementation:

1. User pays successfully in Stripe
2. User returns to app
3. The purchased package is correctly identified
4. User account is updated automatically
5. Usage/messages/credits are allocated correctly
6. Profile/subscription shows the new plan clearly
7. The system handles refresh/repeat safely
8. No manual admin intervention is needed

==================================================
DELIVERABLE
==================================================

At the end report exactly:

1. how the purchased package is identified after Stripe checkout
2. what metadata is passed into Stripe
3. whether webhook or another verified backend fulfillment path is used
4. how the user account is updated
5. where the purchased plan/remaining usage is shown in the UI
6. what files were changed
7. any limitation that still remains