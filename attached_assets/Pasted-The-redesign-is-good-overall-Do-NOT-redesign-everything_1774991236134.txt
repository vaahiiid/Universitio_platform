The redesign is good overall. Do NOT redesign everything again.
Do NOT break the working app-shell, chat model, notifications, or current user/admin flows.

Only fix these 3 remaining issues carefully and completely.

==================================================
ISSUE 1 — USER CHAT HEADER ACTIONS TAKE TOO MUCH SPACE
==================================================

Current problem:
On the user chat screen, "End Chat" and "New Chat" are still placed too high in the chat area and cause unnecessary vertical space / scrolling.
The user wants the chat to feel like a real web app.

Required behavior:
1. Remove "End Chat" and "New Chat" from the upper chat area
2. Keep only the main top navbar/header with the logo at the top
3. Move "End Chat" and "New Chat" down into a lower action area together with the other chat actions
4. The top of the chat should be visually clean:
   - only navbar/topbar/logo
   - no extra action row consuming vertical space
5. Result:
   - chat should not get extra scroll because of these actions
   - first view should feel like a real app chat screen

Important:
- Do NOT remove End Chat or New Chat functionality
- Only reposition them into a better place in the UI

==================================================
ISSUE 2 — USER NEW MESSAGE VISIBILITY + CLICKABLE NOTIFICATION
==================================================

There are 2 parts here.

PART A — Inside chat:
If the user is scrolled up / above the latest messages and a new mentor/admin message arrives:
- show a clear "New message" indicator inside the chat
- make it obvious that a new message arrived
- do NOT rely only on silent append

PART B — Outside chat:
If the user is in another dashboard section and a notification appears:
- the notification must be clickable
- clicking it should take the user directly to the relevant chat/conversation
- it should effectively lead the user to the unread/new message
- unread/seen state should update appropriately after the user opens/views the chat

Requirements:
1. Keep the current visible notification behavior that already works
2. Make the notification clickable
3. Clicking the notification must:
   - switch to Chat
   - open the relevant conversation
   - bring the user to the new/unread message context
4. Inside chat, the "New message" indicator must appear when user is not at the bottom

==================================================
ISSUE 3 — ADMIN MOBILE RESPONSIVENESS IS STILL BROKEN
==================================================

Current problem:
The admin mobile experience is not truly responsive.
Problems include:
- send does not work properly on mobile
- sizing is not mobile-sized
- the chat container is larger than the mobile viewport
- the layout still behaves like a squeezed desktop UI

Required behavior:
1. Make the admin messaging interface truly mobile responsive
2. On mobile:
   - the UI must fit the viewport
   - no oversized chat panel
   - no horizontal overflow
   - input area must be usable
   - send must work correctly
3. Implement a proper mobile pattern:
   - user list view
   - tap a user → open chat view
   - back button returns to list
4. Message area and input must fit naturally on mobile
5. The admin mobile chat should feel like a messaging app, not a broken desktop layout

==================================================
STRICT RULES
==================================================

- Do NOT redesign the whole app again
- Do NOT break current user notifications
- Do NOT break current active/archived chat logic
- Do NOT break rename/delete/end chat
- Do NOT break desktop behavior unless required for consistency
- Only fix these 3 issues

==================================================
DELIVERABLE
==================================================

At the end report exactly:
1. where End Chat and New Chat were moved in the user chat UI
2. how the in-chat "New message" indicator works
3. how the clickable notification now routes the user to the correct chat/message
4. what caused admin mobile send/layout issues
5. how admin mobile responsiveness was fixed
6. what files were changed
7. confirm no unrelated working behavior was changed